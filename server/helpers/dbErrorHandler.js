const getUniqueErrorMessage = err => {
    let output;

    try
    {
        let fieldName = err.message.substring()
    }
    catch(ex)
    {

    }
}

const getErrorMessage = err => {    
    let message = '';

    if (err.code)
    {
        switch (err.code)
        {
            case 11000:
            case 11001:
        }
    }
}